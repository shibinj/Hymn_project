// Firebase configuration template
// Copy this file to firebase-config.js and fill in your values.
// Get values from: Firebase Console → Project Settings → Your Apps → Web App → SDK setup
//
// firebase-config.js is gitignored — never commit actual keys.

export const firebaseConfig = {
    apiKey: "YOUR_API_KEY",
    authDomain: "YOUR_PROJECT.firebaseapp.com",
    projectId: "YOUR_PROJECT",
    storageBucket: "YOUR_PROJECT.firebasestorage.app",
    messagingSenderId: "YOUR_SENDER_ID",
    appId: "YOUR_APP_ID"
};

export const ALLOWED_ADMINS = [
    "your-admin@gmail.com",
    // add more if needed
];
